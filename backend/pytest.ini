[pytest]
asyncio_mode = auto
pythonpath = .
; test_connection.py at the repo root is a manual DB-connectivity script,
; not a pytest test — scope collection to the real test package.
testpaths = app/tests
